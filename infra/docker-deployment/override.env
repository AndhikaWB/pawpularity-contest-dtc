# Will override variables set in the root .env file
# Should only be used when running Docker locally

# -----

AWS_ENDPOINT_URL="http://juicefs:9000"
LAKECTL_SERVER_ENDPOINT_URL="http://lakefs:8000"
MLFLOW_TRACKING_URI="http://mlflow:5000"
REPORT_DB_HOST="http://postgres:5432"
SERVER_MODEL_ENDPOINT="http://pawpaw-serving:8765/predict"